# Windows and Windows Server operator notes

Use Windows or Windows Server as an admin workstation for Git, SSH, PowerShell, kubectl, Helm, and Argo CD CLI operations.

Do not store production kubeconfigs or private keys in this repository.
