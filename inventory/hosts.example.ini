# Copy to inventory/hosts.local.ini and edit the copy only.
# Do not commit real IP addresses or hostnames.

[rke2_servers]
node-1 ansible_host=<NODE_1_IP> ansible_user=<SSH_USER>
node-2 ansible_host=<NODE_2_IP> ansible_user=<SSH_USER>
node-3 ansible_host=<NODE_3_IP> ansible_user=<SSH_USER>

[rke2_servers:vars]
rke2_api_vip=<VIP_ADDRESS>
rke2_api_dns=<VIP_DNS_NAME>
rke2_ingress_vip=<INGRESS_VIP_ADDRESS>
rke2_platform_domain=<PLATFORM_DOMAIN>
# Optional. If omitted or left as a placeholder, ansible/playbooks/install-rke2.yml
# generates and reuses a private controller-side token at ~/.config/platform-gitops/rke2-token.
rke2_token=<GENERATE_WITH_PASSWORD_MANAGER>

# Optional pin. Example format: v1.35.4+rke2r1
# rke2_version=<RKE2_VERSION>
rke2_channel=stable
rke2_cni=cilium

# Optional timeouts in seconds.
# rke2_install_timeout=1200
# rke2_install_poll_interval=15
# rke2_start_timeout=300
# rke2_start_poll_interval=10
# rke2_api_ready_timeout=300
# rke2_node_ready_timeout=600
# rke2_verify_timeout=120

# Optional online install registry checks. Disable only when a private registry
# mirror or airgap image flow is already configured.
# rke2_registry_check_enabled=true
# rke2_registry_check_timeout=45
# rke2_registry_connect_timeout=10
# rke2_registry_check_urls=["https://registry-1.docker.io/v2/","https://auth.docker.io/token","https://production.cloudflare.docker.com/"]

# Optional enterprise proxy. Values may also be supplied as RKE2_HTTP_PROXY,
# RKE2_HTTPS_PROXY, and RKE2_NO_PROXY environment variables from the controller.
# Proxy values can contain credentials, so prefer ignored local inventory or a
# private secret workflow.
# rke2_http_proxy=http://proxy.example.com:8080
# rke2_https_proxy=http://proxy.example.com:8080
# rke2_no_proxy=<LOOPBACK>,localhost,<RFC1918_CIDRS>,<NODE_1_IP>,<NODE_2_IP>,<NODE_3_IP>,<VIP_ADDRESS>,<VIP_DNS_NAME>
