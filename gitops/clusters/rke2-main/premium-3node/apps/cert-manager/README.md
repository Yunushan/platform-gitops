# cert-manager

Premium profile certificate lifecycle component. The profile runs multiple
controller, webhook, and cainjector replicas and leaves private issuers to
private overlays.

Use trust-manager for trust bundle distribution and optional step-ca only when
an internal CA is required.
