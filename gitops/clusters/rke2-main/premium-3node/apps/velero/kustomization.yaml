apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: velero
resources:
  - namespace.yaml
helmGlobals:
  chartHome: charts/velero-12.1.0
helmCharts:
  - name: velero
    version: 12.1.0
    releaseName: velero
    namespace: velero
    valuesFile: values.yaml
    includeCRDs: true
