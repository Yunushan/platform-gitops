<<<<<<< HEAD
crds:
  enabled: true

=======
# Monitoring bootstrap profile rendered by scripts/render_private_platform_values.py.
# Uses persistent Grafana SQLite for first deployment. Switch Grafana to external
# PostgreSQL for long-term HA.
>>>>>>> c06f7fb (Render platform app values and app secrets)
prometheus:
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - prometheus-gitops-arge.isbak.com.tr
    tls:
      - secretName: prometheus-tls
        hosts:
          - prometheus-gitops-arge.isbak.com.tr
  prometheusSpec:
    replicas: 2
    retention: 15d
    retentionSize: 20GB
    podMonitorSelectorNilUsesHelmValues: false
    serviceMonitorSelectorNilUsesHelmValues: false
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: longhorn-standard
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 50Gi

alertmanager:
  enabled: true
  alertmanagerSpec:
    replicas: 3
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: longhorn-standard
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 10Gi

grafana:
  replicas: 1
  persistence:
    enabled: true
    type: pvc
    storageClassName: longhorn-standard
    accessModes:
      - ReadWriteOnce
    size: 10Gi
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - grafana-gitops-arge.isbak.com.tr
    tls:
      - secretName: grafana-tls
        hosts:
          - grafana-gitops-arge.isbak.com.tr

defaultRules:
  create: true

kubeEtcd:
  enabled: true

kubeScheduler:
  enabled: true

kubeControllerManager:
  enabled: true
