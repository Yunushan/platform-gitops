# traefik

Default ingress controller for the recommended Rocky Linux 10 + RKE2 platform stack.

Customize real hostnames, TLS, and MetalLB service addresses through private overlays or encrypted secrets.
