# TypeScript service scaffold

Minimal TypeScript starter for CI testing and containerization.

## Test

```bash
node -e "console.log('hello from typescript placeholder')"
```

## CI/CD

Use Woodpecker, GitHub Actions, GitLab CI, Gitea Actions, or Forgejo Actions from `examples/service-template` as the base pipeline.
