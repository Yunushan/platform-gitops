# Kotlin service scaffold

Minimal Kotlin starter for CI testing and containerization.

## Test

```bash
echo "compile with kotlinc in your final image"
```

## CI/CD

Use Woodpecker, GitHub Actions, GitLab CI, Gitea Actions, or Forgejo Actions from `examples/service-template` as the base pipeline.
